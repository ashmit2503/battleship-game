"""Django models for the battleship game application.

Currently uses MongoDB for data storage, so Django models are minimal.
User authentication is handled through Django's built-in User model.
"""

from django.db import models
